{
  "name": "kanban-serverless",
  "version": "1.0.0",
  "description": "Sistema Kanban Serverless para Vercel",
  "scripts": {
    "build": "echo 'No build step required'",
    "install": "npm install"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  },
  "engines": {
    "node": ">=18.x"
  }
}
